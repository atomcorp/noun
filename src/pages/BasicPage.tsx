import React from 'react';
import css from './BasicPage.css';
const BasicPage = () => (
  <section className={css.page}>
    <header>
      <div>Logo</div>
      <nav>Nav</nav>
    </header>
    <section>Body</section>
  </section>
);
