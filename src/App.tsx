import React from 'react';
import Button from './components/Button/Button';

const Page = () => (
  <section>
    <header>
      <div>Logo</div>
      <nav>Nav</nav>
    </header>
    <section>Body</section>
    <Button />
  </section>
);

export default Page;
